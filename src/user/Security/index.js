import SecurityIcon from '@material-ui/icons/Security';

import list from './List';
import edit from './Edit';

// eslint-disable-next-line import/no-anonymous-default-export
export default { icon: SecurityIcon, list, edit };
